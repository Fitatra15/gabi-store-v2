{
  "name": "gabi-store-v2",
  "version": "1.0.0",
  "description": "Gabi-Store - Marketplace Antsiranana",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node src/app.js",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
